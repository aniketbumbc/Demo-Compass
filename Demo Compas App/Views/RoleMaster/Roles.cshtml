@model IEnumerable<Demo_Compas_App.Models.RoleMaster>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table" border="1">
    <tr>
      <th>
           @Html.DisplayNameFor(model => model.RoleId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RoleName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RoleDesc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RoleStatus)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
      <td>
            @Html.DisplayFor(modelItem => item.RoleId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RoleName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RoleDesc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RoleStatus)
        </td>
        <td>
            @Html.ActionLink("Edit Role", "Edit", new {id=item.RoleId }) |
            @Html.ActionLink(" See Details", "Details", new { id = item.RoleId }) |
            @Html.ActionLink("Delete Role", "Delete", new { id = item.RoleId })
        </td>
    </tr>
}

</table>
